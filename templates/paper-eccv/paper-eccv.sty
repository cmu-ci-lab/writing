\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{paper-eccv}[2026/08/04 v1.0 CIRL ECCV paper macros]

% remove conflict on proof environment between llncs and mathdefs
\let\proof\relax
\let\endproof\relax

\RequirePackage[eccv,review,year=2026,ID=*****]{cirl}
\RequirePackage[accsupp]{axessibility}	% improve PDF readability for those with disabilities
\RequirePackage[pagebackref,breaklinks,colorlinks,citecolor=eccvblue]{hyperref}

\graphicspath{{figures/}}

% Commenting macros. Add one per author, and delete them all before submitting.
\newcommand{\igkiou}[1]{{\color{red}{[igkiou: #1]}}}

% Notation macros. Define the notation of this paper here, so that changing it
% later is one edit rather than a search across the whole document.
